<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <h3 class="navbar-text"><%= @user.email %> favourites</h3>
    </div>
  </div>
</nav>

<% @favourites.each do |station| %>
  <%= link_to station.name, station_info_path(:data => station.station) %> <%= link_to 'Delete', station, method: :delete, data: { confirm: "You sure?"} %>
<% end %>