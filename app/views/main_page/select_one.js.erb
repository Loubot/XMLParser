
    alert("<%= returned_station %>")
    var map, mapOptions;
    mapOptions = {
      center: new google.maps.LatLng(-34.397, 150.644),
      zoom: 8
    };
    return map = new google.maps.Map(document.getElementById("map"), mapOptions);
  